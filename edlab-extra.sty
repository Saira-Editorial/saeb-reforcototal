% edlab-extra.sty

\newcommand{\blankpage}{\newpage\hbox{~}\thispagestyle{empty}\newpage}


% url.sty
% You can switch the style of printing using “\urlstyle{xx}”, where “xx” can be
% any defined style. The pre-defined styles are “tt”, “rm”, “sf” and “same” which all
% allow the same linebreaks but use different fonts — the first three select a specific
% font and the “same” style uses the current text font. You can define your own
% styles with different fonts and/or line-breaking by following the explanations

\urlstyle{tt} % {same} mesma fonte; {sf} sem serifa 

% Epigraph (memoir)
\epigraphfontsize{\footnotesize}			% tamanho da fonte
\setlength{\epigraphrule}{0pt}				% tira a linha
% \setlength{\epigraphwidth}{.6\textwidth}	% aumenta o tamanho da epígra
% \epigraphtextposition{flushright}			
% \epigraphtextposition{flushleft}
% \epigraphsourceposition{flushright} 

% Enumerate (enumitem.sty)
    % • Vertical spacing:
    %     – topsep
    %     – partopsep
    %     – parsep
    %     – itemsep
    % • Horizontal spacing:
    %     – leftmargin
    %     – rightmargin
    %     – listparindent
    %     – labelwidth
    %     – labelsep
    %     – itemindent

\setlist[itemize,1]{label={$\triangleright$},leftmargin=5pt,itemsep=0pt,parsep=0pt}

% Quote (latex.ldt)
\renewenvironment{quote}{%
  \begin{list}{}{%
    \setlength{\listparindent}{3ex}%
    \setlength{\labelsep}{0mm}%
    \setlength{\leftmargin}{0mm}%
    \setlength{\rightmargin}{0mm}%
    \setlength{\topsep}{1ex}
    \setlength{\partopsep}{0mm}
    \setlength{\parskip}{0mm}
    \setlength{\lineskip}{0mm}
    \setlength{\parsep}{0ex}%
    }%
    \small%
    \item%
}{%
  \end{list}%
}


% Comando para fazer subtítulo
\newcommand{\subtitulo}[1]{\NoCaseChange{\textnormal{\break\Large\itshape#1}}}
\newcommand{\chapterspecial}[2]{%
 \chapter[#1]%
     {#1\ifx#2\relax\else\subtitulo{#2}\fi}%
\markright{#1}}

% Definindo as cores CMYK
\definecolor{prof}{cmyk}{0,1,0,0}
\newcommand{\prof}[1]{
  \begin{minipage}[t]{\linewidth}%
\raggedleft\color{prof}\small #1
  \end{minipage}%
}

\definecolor{aluno}{cmyk}{1,0.85,0.27,0.1}
\newcommand{\aluno}[1]{\textcolor{aluno}{#1}}
\newcommand{\colorsec}[1]{%
    \section{\textcolor{aluno}{#1}}%
}


\definecolor{exercicio}{cmyk}{0,0.6,0.45,0}
\newcommand{\exercicio}[1]{\textcolor{exercicio}{#1}}



\usepackage{amssymb}
\usepackage{paralist}
\usepackage{wrapfig}
\usepackage{tikzpagenodes}

% Definir novo ambiente para lista com caixas de seleção
\newlist{boxlist}{enumerate}{1}
\setlist[boxlist]{label=\textcolor{exercicio}{[}\mbox{ }\mbox{ }\textcolor{exercicio}{]},font=\bfseries}

% Definir novo ambiente para parágrafo lateral
\newcommand{\sidetext}[1]{%
  \begin{tikzpicture}[remember picture,overlay]
    \node [anchor=north east, text width=3cm, align=justify, align=right, font=\small] 
          at (current page text area.north east) 
          {\parbox[t]{\textwidth}{\textcolor{prof}{#1}}};

  \end{tikzpicture}%
}


% Comando para fazer as linhas de respostas
\newcommand{\mylines}{%
  \par\noindent\makebox[\linewidth]{%
    \begin{tikzpicture}
      \colorlet{mycolor}{exercicio}
      \draw[thick, color=mycolor] (0,0) -- (\linewidth,0);
      \draw[thick, color=mycolor, yshift=-1cm] (0,0) -- (\linewidth,0);
      \draw[thick, color=mycolor, yshift=-2cm] (0,0) -- (\linewidth,0);
    \end{tikzpicture}%
  }\par%
}